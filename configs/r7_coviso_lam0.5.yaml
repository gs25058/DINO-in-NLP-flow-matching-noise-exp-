# R7 cov_iso 본 실험 (METHOD.md §9, results/analysis/coviso/report.md). 캘리브레이션
# dry-run(200 step, r7_coviso_calib)에서 λ=1.0이 안정적으로 수렴함을 확인한 뒤 그 주변으로
# 소(0.5)/중(1.0)/대(2.0) 3개 후보를 잡았다. KoLeo는 명시적으로 꺼서 중첩을 방지한다.
extends: r5d_combined.yaml

run_name: r7_coviso_lam0.5

loss:
  koleo_lambda: 0.0
  cov_iso_lambda: 0.5
  cov_ema_momentum: 0.99
  cov_iso_start_step: 50
