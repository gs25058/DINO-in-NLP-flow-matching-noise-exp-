# r6_bert_koleo_lrsplit_optuna_best_t27_koleomin0.25와 동일한 hold/decay 일정이되
# koleo_min_ratio=0.0 - 후반(step 900 이후) KoLeo를 완전히 소거하는 극단 검증.
# 목적: 후반에도 소량의 KoLeo가 여전히 필요한지(re-collapse 여부로 판정) 확인.
extends: r6_bert_koleo_lrsplit_optuna_best_t27.yaml

run_name: r6_bert_koleo_lrsplit_optuna_best_t27_koleomin0

loss:
  koleo_hold_frac: 0.2
  koleo_decay_frac: 0.4
  koleo_min_ratio: 0.0
