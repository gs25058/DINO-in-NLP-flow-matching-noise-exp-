# koleo_lambda 감쇠 스케줄 실험 (P-KS1~3, results/analysis/koleo_schedule/report.md).
# 배경: r6_bert_koleo_lrsplit_optuna_best_t27(상수 λ)의 L_koleo가 step~300에서 포화(-0.26
# 평탄)하는데도 반발력이 계속 걸려 uniformity는 거의 못 얻고(-3.17->-3.53) alignment만
# 단조 악화(0.44->0.51), STS가 step 750 피크(0.6995) 후 1499까지 미끄러짐(0.6816).
# 진단: 후반 정규화 과잉. hold_frac=0.2(=step 300)까지 원래 λ 유지해 초반 공간 조직 효과는
# 보존하고, decay_frac=0.4(=step 300~900) 동안 cosine으로 λ/4까지 낮춰 후반 alignment
# 지불을 줄인다.
# 대조군: r6_bert_koleo_lrsplit_optuna_best_t27_s42/s43 (상수 λ, 이미 존재 - 재실행 불필요).
extends: r6_bert_koleo_lrsplit_optuna_best_t27.yaml

run_name: r6_bert_koleo_lrsplit_optuna_best_t27_koleomin0.25

loss:
  koleo_hold_frac: 0.2
  koleo_decay_frac: 0.4
  koleo_min_ratio: 0.25
