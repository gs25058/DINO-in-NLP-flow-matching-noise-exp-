# Part C: r2(붕괴하는 대표)에 진단 로깅 전부 켬. augment/loss/train의 실질 하이퍼파라미터는
# r2_anchor_curriculum.yaml과 동일 - diag_*/dense_early_eval만 추가.
extends: r2_anchor_curriculum.yaml

run_name: r2_diag

train:
  diag_grad_norms: true
  diag_tbin_kl: true
  diag_teacher_eval: true
  diag_confidence: true
  diag_drift: true
  dense_early_eval: true
