# R3: consistency 모드 + curriculum, K=3, Δt=0.1 (METHOD.md §6)
extends: base.yaml

run_name: r3_consistency_curriculum
seed: 42

augment:
  kind: flow_noise
  mode: consistency
  num_student_views: 3
  t_lo: 0.02
  t_start: 0.05
  t_max: 0.5
  warmup_frac: 0.4
  delta_t: 0.1
  time_embed: false

loss:
  teacher_temp: 0.082
  student_temp: 0.151
  center_momentum: 0.920
  velocity_head: false
  velocity_lambda: 0.05
