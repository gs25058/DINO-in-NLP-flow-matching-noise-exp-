# r7_coviso_bert_lam0.5와 동일, cov_iso_lambda=1.0 (캘리브레이션에서 직접 안정성을 확인한 값).
extends: r5d_bert.yaml

run_name: r7_coviso_bert_lam1

loss:
  koleo_lambda: 0.0
  cov_iso_lambda: 1.0
  cov_ema_momentum: 0.99
  cov_iso_start_step: 50
