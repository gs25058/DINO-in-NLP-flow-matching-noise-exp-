# 데이터/캐시 (prepare_data.py로 재생성 가능)
data/
scratch/
.hf_home/
.uv_cache/

# 학습 산출물
results/
checkpoints/
checkpoints_archive/
wandb/

# 프로젝트 지시문/방법 스펙 (내부용, repo에는 불필요)
CLAUDE.md
METHOD.md

# 참고자료 (개인 문서/대용량 중첩 git 저장소, 코드와 무관 - 레포에 불필요)
referecne_files/

# 세션 부산물
*.ipynb_checkpoints/
*claude*.txt

# scripts/tune.py가 trial마다 만드는 임시 config (실행 중 자동 삭제, 혹시 남으면 무시)
configs/tmp*.yaml

# 파이썬/도구
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.pt
*.pth

# uv.lock은 재현성을 위해 반드시 포함 (ignore 금지)
!uv.lock

# 종료된 실험의 config 아카이브 (로컬 보관용, 원격에는 올리지 않는다).
# 결론은 results/analysis/ 리포트에 기록돼 있고, 재현이 필요하면 로컬 아카이브에서 꺼내 쓴다.
configs/archive_modernbert/
configs/archive_bert/
