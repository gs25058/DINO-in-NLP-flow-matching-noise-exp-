# R12-B 대조군: iid 노이즈만 강화 U(0.55, 0.70). 구조는 그대로 두고 "난이도"만 올린다.
# 이 arm이 r12b~e와 함께 개선되면 원인은 구조가 아니라 단순 난이도였다는 뜻이다(P-V3).
extends: r8_bert_coviso_sched_optuna_t50.yaml
run_name: r12a_bert_iid_t0.55-0.70
augment:
  t_lo: 0.55
  t_start: 0.70
  t_max: 0.70
collapse_guard:
  enabled: true
  h_p_bar_t_ref: 9.0097     # r8 plateau(step 675~1499, 2시드) 실측
  eff_rank_ref: 305.16
  batch_kl_ref: 0.0620
