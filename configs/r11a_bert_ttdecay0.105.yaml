# R11-A 개루프: teacher_temp plateau 0.1348 -> 0.105 (plateau의 78%), step 675~1275 cosine.
# 가설: teacher 온도 인하 = 판별 해상도 상승(DINO prototype softmax ≡ InfoNCE 표본 softmax).
# 보수적인 arm - 붕괴 없이 alignment 반응이 나오는지 먼저 본다.
extends: r8_bert_coviso_sched_optuna_t50.yaml
run_name: r11a_bert_ttdecay0.105
loss:
  teacher_temp_decay_start_frac: 0.45
  teacher_temp_decay_end_frac: 0.85
  teacher_temp_final: 0.105
  teacher_temp_decay_shape: cosine
collapse_guard:
  enabled: true
  h_p_bar_t_ref: 9.0097     # r8 plateau(step 675~1499, 2시드) 실측
  eff_rank_ref: 305.16
  batch_kl_ref: 0.0620
