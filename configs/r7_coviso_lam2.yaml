# r7_coviso_lam0.5와 동일, cov_iso_lambda=2.0 (가장 강한 강도). l_iso_mean<=1,
# l_iso_cov<=~1(완전 rank-1일 때)이라 최악의 경우에도 기여가 ~4로 DINO CE(~9.0)를
# 압도하지 않는다는 계산에 근거한 상한 후보.
extends: r5d_combined.yaml

run_name: r7_coviso_lam2

loss:
  koleo_lambda: 0.0
  cov_iso_lambda: 2.0
  cov_ema_momentum: 0.99
  cov_iso_start_step: 50
